from jac.symbol_table import SymbolTable


def test_add():
    ## Given
    table = SymbolTable()
    assert 1 == 1
    # ## When
    # result = _remove_comment(line)
    #
    # ## Then
    # assert len(line) == len(result)
    # for l in result:
    #     assert l == ""
